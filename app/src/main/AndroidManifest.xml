
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Minimal permission set for Play review: mic for hands-free voice listening (the app
         listens automatically — no button press required), internet for the optional AI backend
         proxy. Emergency dialing uses ACTION_DIAL (the system dialer), which needs NO
         CALL_PHONE permission. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Needed for the optional caregiver-configured "timed listening" mode (up to 12h):
         some users (e.g. dementia patients) cannot reliably press a Talk button, so a
         caregiver can opt into a window where the app listens on its own via a
         foreground service. Only requested/started when the caregiver enables it. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Required to prompt the user to exempt this app from battery optimization so
         the timed-listening foreground service isn't killed by Doze mode mid-session.
         Permitted for health/safety apps per Play Store policy. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <!-- Keeps the CPU running while the timed-listening foreground service is active so
         the service isn't suspended when the screen turns off during a multi-hour window. -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Allows [BootReceiver] to restart the timed-listening service after a phone reboot,
         so Mom's listening window is restored even if she cannot operate the app herself.
         Permitted for health/safety apps per Play Store policy. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Haptic feedback on responses — helps users with tremors or vision impairment
         know the app has heard them. Normal vibration; always-on by default. -->
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- For Play Store compliance: declare telephony/mic as not required so the app
         installs on devices without them -->
    <uses-feature android:name="android.hardware.telephony" android:required="false"/>
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MomsAI"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules">

        <activity
            android:name="com.friendai.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".RulesActivity"
            android:exported="false"/>

        <activity
            android:name=".PinActivity"
            android:exported="false"/>

        <activity
            android:name=".EmergencyActivity"
            android:exported="false"/>

        <activity
            android:name=".TroubleshootingActivity"
            android:exported="false"/>
        <activity
            android:name=".VipActivity"
            android:exported="false"/>
        <activity
            android:name=".VipStatusActivity"
            android:exported="false"/>

        <service
            android:name=".TimedListeningService"
            android:exported="false"
            android:foregroundServiceType="microphone"/>

        <!-- Restarts [TimedListeningService] on boot so timed-listening continues after
             a phone reboot without requiring Mom to touch the app. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>
